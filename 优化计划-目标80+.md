# BI-Dashboard 优化计划 — 目标：全维度80+

> 当前评分：安全性42 | 可维护性55 | 架构设计68 | 功能完整度72
> 目标评分：安全性80+ | 可维护性80+ | 架构设计80+ | 功能完整度80+

---

## 一、安全性：42 → 80+

### 1.1 密码加密（+15分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/app/models/user.py` |
| 操作 | 新增 `password_hash = Column(String(255))` 字段 |
| 文件 | `backend/app/api/v1/auth.py` |
| 操作 | 登录验证改为 `verify_password(form.password, user.password_hash)` |
| 文件 | `backend/app/services/permission_service.py` |
| 操作 | `create_user()` 改为 `user.password_hash = get_password_hash(password)` |
| 文件 | `backend/app/core/security.py` |
| 操作 | 确认 `verify_password()` 和 `get_password_hash()` 已正确实现 bcrypt |
| 文件 | 新建 `backend/migrate_password_hash.py` |
| 操作 | 一次性迁移：遍历 User 表，明文→bcrypt hash，存入 password_hash |
| 验证 | 登录成功 + 旧密码仍可用 + 数据库无明文密码 |

### 1.2 SQL参数化（+12分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/app/services/chart_service.py` |
| 操作 | `_metric_filter_sql_clauses()` 返回 `(sql_fragment, params_list)` 元组，值用 `%s` 占位 |
| 操作 | `execute_chart_query()` 改为 `await conn.execute(text(sql), params)` |
| 操作 | `_resolve_relative_date()` 返回值带参数 |
| 文件 | `backend/app/services/pipeline/temp_table_manager.py` |
| 操作 | 表名列名加白名单函数 `_validate_identifier(name)` 只允许 `[a-zA-Z0-9_]` |
| 操作 | INSERT/CREATE 中的值改为参数绑定 |
| 文件 | `backend/app/services/pipeline/trigger_scheduler.py` |
| 操作 | `_has_new_data()` 中字段名白名单校验 + 值参数化 |
| 验证 | 图表查询正常 + 过滤器正常 + Pipeline执行正常 |

### 1.3 SECRET_KEY外移（+5分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/.env` → 重命名为 `backend/.env.example` |
| 操作 | 值替换为占位符 `SECRET_KEY=your-secret-key-here` |
| 文件 | `backend/.gitignore` |
| 操作 | 添加 `.env` 行 |
| 操作 | 执行 `git rm --cached backend/.env` |
| 验证 | `git status` 不再追踪 .env |

### 1.4 移除前端敏感依赖和日志（+3分）

| 项目 | 内容 |
|------|------|
| 文件 | `frontend/bi-dashboard/package.json` |
| 操作 | 删除 `"mysql2": "^3.16.3"` 行，执行 `npm uninstall mysql2` |
| 文件 | `frontend/bi-dashboard/src/services/authService.ts` |
| 操作 | 删除所有 `console.log` 行（约15处） |
| 文件 | `frontend/bi-dashboard/src/contexts/AuthContext.tsx` |
| 操作 | 删除所有 `console.log` 行 |
| 验证 | 前端 build 无报错 + 浏览器控制台无敏感信息输出 |

### 1.5 数据源密码加密存储（+3分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/app/models/data_source.py` |
| 操作 | `password` 字段存储前加密，读取时解密 |
| 文件 | 新建 `backend/app/core/crypto.py` |
| 操作 | 使用 Fernet 对称加密，密钥从环境变量读取 `ENCRYPTION_KEY` |
| 文件 | `backend/app/api/v1/datasources.py` |
| 操作 | 创建数据源时加密密码，返回时脱敏 |
| 验证 | 数据库中 password 列无明文 + 连接测试仍正常 |

---

## 二、可维护性：55 → 80+

### 2.1 拆分 engine.py God Class（+10分）

| 项目 | 内容 |
|------|------|
| 拆分方案 | `PipelineEngine` 仅保留 `run()` 编排逻辑 |
| 新文件 | `backend/app/services/pipeline/sql_builder.py` — SQL构建、列投影、行过滤、列重命名 |
| 新文件 | `backend/app/services/pipeline/step_executor.py` — 步骤执行、批处理、临时表写入 |
| 新文件 | `backend/app/services/pipeline/type_inferrer.py` — 列类型推断、规范节点类型 |
| 新文件 | `backend/app/services/pipeline/validator.py` — SQL校验、拓扑排序、节点验证 |
| 迁移方式 | 逐步迁移：先抽 validator → 再抽 sql_builder → 再抽 type_inferrer → 最后抽 step_executor |
| 每步验证 | 迁移一个类后，跑完整 Pipeline 创建→执行→预览流程 |

### 2.2 自定义异常体系（+5分）

| 项目 | 内容 |
|------|------|
| 新文件 | `backend/app/exceptions.py` |
| 内容 | `AppException`基类、`NotFoundError`、`PermissionDeniedError`、`ValidationError`、`DatabaseError`、`PipelineExecutionError` |
| 文件 | `backend/app/main_optimized.py` |
| 操作 | 添加全局异常处理器，统一 JSON 错误响应格式 |
| 文件 | `backend/app/services/*.py` 所有 service 文件 |
| 操作 | `raise Exception(str(e))` → 对应自定义异常类 |
| 验证 | API 返回统一格式 `{"detail": {"code": "...", "message": "..."}}` |

### 2.3 引入 Alembic 数据库迁移（+5分）

| 项目 | 内容 |
|------|------|
| 操作 | `pip install alembic`，`alembic init alembic` |
| 文件 | `backend/alembic/env.py` |
| 操作 | 配置 async session + 导入所有 models |
| 文件 | `backend/alembic.ini` |
| 操作 | `sqlalchemy.url` 改为从环境变量读取 |
| 操作 | `alembic revision --autogenerate -m "initial"` |
| 操作 | 删除 `migrate_add_pipeline_fields.py` 等迁移脚本 |
| 验证 | `alembic upgrade head` 成功 + 表结构正确 |

### 2.4 User.userID 改自增（+3分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/app/models/user.py` |
| 操作 | `userID = Column(Integer, primary_key=True, autoincrement=True)` |
| 文件 | `backend/app/services/permission_service.py` |
| 操作 | `create_user()` 删除 `SELECT MAX(userID)+1` 逻辑 |
| 验证 | 多用户并发创建不冲突 |

### 2.5 清理空文件和死代码（+2分）

| 项目 | 内容 |
|------|------|
| 操作 | 删除 `backend/app/api/v1/endpoints/user.py`（空文件） |
| 操作 | `backend/app/models/__init__.py` 添加所有 model 的显式导入 |
| 操作 | `backend/app/schemas/__init__.py` 添加所有 schema 的显式导入 |
| 操作 | 删除 `backend/main.py`（已被 `main_optimized.py` 替代） |

---

## 三、架构设计：68 → 80+

### 3.1 解耦循环依赖（+5分）

| 项目 | 内容 |
|------|------|
| 新文件 | `backend/app/core/events.py` — 事件总线 |
| 内容 | 定义事件类型：`ChartArchived`、`PipelineUpdated`、`DashboardModified` |
| 操作 | Services 之间不再直接 import，改为发布/订阅事件 |
| 示例 | `PipelineService.update_pipeline()` 发布 `PipelineUpdated` 事件 → `ChartService` 订阅并处理 archive |
| 验证 | 无 inline import（`from app.services.xxx import Yyy` 不再出现在方法体内） |

### 3.2 统一分页和响应格式（+3分）

| 项目 | 内容 |
|------|------|
| 新文件 | `backend/app/schemas/common.py` |
| 内容 | `PageRequest(skip:int=0, limit:int=20)`、`PageResponse(items:list, total:int, page:int, size:int)` |
| 文件 | 所有 API endpoints |
| 操作 | 列表接口统一返回 `PageResponse` 格式 |
| 验证 | 所有列表 API 返回 `{items, total, page, size}` |

### 3.3 配置外部化（+2分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/app/core/config.py` |
| 操作 | 新增配置项：`CHART_QUERY_LIMIT`(默认10000)、`CACHE_TTL`(默认3600)、`PIPELINE_BATCH_SIZE`(默认5000)、`TEMP_TABLE_RETENTION_MINUTES`(默认60) |
| 文件 | `backend/app/services/chart_service.py` |
| 操作 | 硬编码 `10000` → `settings.CHART_QUERY_LIMIT` |
| 文件 | `backend/app/services/pipeline/engine.py` |
| 操作 | 硬编码 `5000` → `settings.PIPELINE_BATCH_SIZE` |

### 3.4 临时表安全机制（+2分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/app/services/pipeline/temp_table_manager.py` |
| 操作 | 新增 `cleanup_all_expired()` 方法，启动时调用一次 |
| 文件 | `backend/app/main_optimized.py` |
| 操作 | lifespan 启动时调用 `TempTableManager.cleanup_all_expired()` |
| 操作 | scheduler 每小时执行一次清理 |
| 验证 | 遗留临时表在服务重启时自动清理 |

---

## 四、功能完整度：72 → 80+

### 4.1 完成 RealTimeSyncService（+4分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/app/services/realtime_sync.py` |
| 操作 | 实现 MySQL binlog 监听（用 `pymysqlreplication` 库）或轮询检测变更 |
| 操作 | `subscribe_to_table(table_name)` → 注册监听 → 变更时推送 WebSocket |
| 操作 | `start()` → 启动监听线程 |
| 操作 | `stop()` → 清理监听器和连接 |
| 文件 | `backend/requirements.txt` |
| 操作 | 添加 `pymysqlreplication` |
| 验证 | 修改数据 → 前端 WebSocket 收到推送自动刷新 |

### 4.2 完善空 API 端点（+2分）

| 项目 | 内容 |
|------|------|
| 文件 | `backend/app/api/v1/endpoints/user.py` |
| 操作 | 实现 `GET /users`（列表）、`GET /users/{id}`（详情）、`PUT /users/{id}`（更新） |
| 文件 | `backend/app/services/permission_service.py` |
| 操作 | 新增 `list_users()`、`get_user()`、`update_user()` 方法 |
| 验证 | 用户管理页面 CRUD 完整可用 |

### 4.3 datetime.utcnow 弃用修复（+2分）

| 项目 | 内容 |
|------|------|
| 操作 | 全局搜索 `datetime.utcnow()` 替换为 `datetime.now(timezone.utc)` |
| 涉及文件 | `permission_service.py`、`pipeline_service.py`、`dashboard_service.py`、`chart_service.py` 等 |
| 验证 | Python 3.12+ 无 DeprecationWarning |

---

## 执行顺序

| 阶段 | 任务 | 预计影响 | 预计耗时 |
|------|------|----------|----------|
| 第1周 | 1.1密码加密 + 1.2 SQL参数化 + 1.3 SECRET_KEY外移 | 安全性 42→74 | 2天 |
| 第1周 | 1.4 前端敏感清理 + 1.5 数据源密码加密 | 安全性 74→80+ | 1天 |
| 第2周 | 2.2 自定义异常 + 2.4 userID自增 + 2.5 清理死代码 | 可维护性 55→65 | 1天 |
| 第2周 | 2.3 引入Alembic | 可维护性 65→70 | 1天 |
| 第3周 | 2.1 拆分engine.py | 可维护性 70→80+ | 3天 |
| 第3周 | 3.2 统一分页 + 3.3 配置外部化 + 3.4 临时表安全 | 架构 68→75 | 1天 |
| 第4周 | 3.1 解耦循环依赖 | 架构 75→80+ | 2天 |
| 第4周 | 4.1 RealTimeSync + 4.2 完善端点 + 4.3 datetime修复 | 功能 72→80+ | 2天 |

**总预计：4周，安全性立即见效，可维护性第二周见效，架构和功能第三四周到位。**
