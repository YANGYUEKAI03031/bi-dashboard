# BI-Dashboard 后端代码质量 — 二次审计 & 提升计划v2

> 审计时间: 2026-05-25 | 项目规模: 54文件 / 16,519行 / 436函数
> 上次评分: 53.5 → 本次评分: 59.5

---

## 一、评分对比

| 维度 | 上次 | 本次 | 变化 | 说明 |
|------|------|------|------|------|
| 安全性 | 52 | 58 | +6 | 部分SQL注入修复，但新文件引入新注入点 |
| 可维护性 | 48 | 54 | +6 | engine.py拆出部分，double fetchone修复 |
| 架构设计 | 58 | 60 | +2 | 新增sql_expressions/graph_builder，但DATABASE_URL BUG未修 |
| 代码规范 | 55 | 62 | +7 | print清除、DEBUG改False、类型提示微升 |
| 性能优化 | 60 | 63 | +3 | 配置项提取、临时表配置可调 |
| **总分** | **53.5** | **59.5** | **+6** | 进步有限，2个CRITICAL BUG和1个新严重注入拉分 |

---

## 二、已修复项 (7/13)

| 编号 | 问题 | 状态 | 证据 |
|------|------|------|------|
| S-01 | JWT SECRET_KEY启动校验 | ✅ 已修 | config.py L87-92, sys.exit(1) |
| S-03 | watermark_manager安全标识符 | ✅ 已修 | 白名单正则+长度限制+点号支持 |
| S-05 | ENCRYPTION_KEY空值警告 | ✅ 已修 | config.py L94-98 |
| S-06 | DEBUG=True默认值 | ✅ 已修 | 改为False |
| S-07 | engine.py 12处print() | ✅ 已修 | 0处残留 |
| S-08 | chart_service WHERE值转义 | ✅ 已修 | _sql_escape_sql_string()已应用 |
| M-04 | double fetchone() bug | ✅ 已修 | 单次fetchone() |

---

## 三、部分修复项 (4/13)

### S-02 temp_table_manager SQL注入 — 部分修复

**已修**: 新增 `_safe_table_name()` (L141-153)、`_validate_step_id()` (L47-54)、`_validate_identifier()` (L57-67)，主力查询用了这些方法。

**遗留**: L657孤儿表清理仍用原始f-string:
```python
# temp_table_manager.py:657
await self.connection.execute(text(f"DROP TABLE IF EXISTS `{tbl}`"))
```
`tbl` 从 `information_schema.tables` 查得，虽风险较低但应统一走 `_safe_table_name()`。

**修复方案**:
```python
# L657 替换为:
await self.connection.execute(text(f"DROP TABLE IF EXISTS {self._safe_table_name(tbl)}"))
```
注意 `_safe_table_name()` 已包裹反引号，DROP语句可正常使用。

---

### S-04 数据源密码加密 — 部分修复

**已修**: datasources.py创建时调 `encrypt_password()`，chart_service.py读取时调 `decrypt_password()`。

**遗留**:
1. 模型层 `password = Column(String(255))` — 列宽不够Fernet密文
2. 更新数据源的API可能遗漏加密
3. crypto.py `_is_plaintext()` 靠 `gAAAAA` 前缀判断（脆弱）
4. 解密失败时直接返回密文当明文用（crypto.py L89-96）

**修复方案**:
```python
# 1. visualization.py — Database模型扩大列宽
password = Column(String(512), nullable=True)  # 512容纳Fernet密文

# 2. 检查所有写入password的位置是否都经过encrypt_password()
# 在datasources.py的update接口中也要加密
if payload.password is not None:
    db_model.password = encrypt_password(payload.password)

# 3. crypto.py — 解密失败时抛异常而非静默回退
except InvalidToken:
    logger.error("密码解密失败，密文可能已损坏或ENCRYPTION_KEY已变更")
    raise ValueError("密码解密失败，请重新配置数据源密码")
```

---

### S-09 validator.py循环导入 — 部分修复

**已修**: validator.py有了独立的 `canonical_pipeline_node_type()` (L18-44)。

**遗留**: sql_expressions.py L159-163 又定义了一份 (`_PIPELINE_NODE_TYPE_CANON`)，engine.py L2456-2458 委托给sql_expressions版本。两份映射可能漂移不同步。

**修复方案**:
```python
# sql_expressions.py — 删除本地 _PIPELINE_NODE_TYPE_CANON 和 canonical_pipeline_node_type
# 改为从validator导入
from app.services.pipeline.validator import canonical_pipeline_node_type

# engine.py — 也从validator导入
from app.services.pipeline.validator import canonical_pipeline_node_type

@staticmethod
def _canonical_pipeline_node_type(node_type: str) -> str:
    return canonical_pipeline_node_type(node_type)
```

---

### M-01 engine.py God Class — 部分修复

**已修**: 拆出 `sql_expressions.py`(1055行) 和 `graph_builder.py`(114行)，engine.py从3835→3196行（减少16.7%）。

**遗留**: 3196行仍然偏大，56个方法仍是God Class。plan中建议拆到~1900行。

**后续拆分建议**（不急，可放在第三批）:
- `preview.py`: preview_node + _preview_infer_output_columns (~400行)
- `step_executor.py`: _execute_step_via_insert_select + _apply_incremental_condition + _update_step_progress (~200行)

---

## 四、未修复项 (2/13) — CRITICAL

### ⛔ A-01: DATABASE_URL 用 *** 当密码 — 应用连不上数据库

**文件**: config.py L28-31

```python
@property
def DATABASE_URL(self) -> str:
    return (
        f"mysql+aiomysql://{self.MYSQL_USER}:***@{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
    )
```

同步版也一样(L35-40):
```python
@property
def DATABASE_URL_SYNC(self) -> str:
    return (
        f"mysql+pymysql://{self.MYSQL_USER}:***@{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
    )
```

**使用此URL的位置（3处，全部连不上DB）**:
- `db/session.py:12` — 主引擎
- `api/v1/pipeline.py:321` — pipeline引擎
- `services/pipeline/trigger_scheduler.py:63` — 调度引擎

**如果系统现在能运行，说明MySQL没密码或密码为空。** 一旦MySQL设了密码，整个应用立刻崩溃。

**修复方案**:
```python
# config.py — 拆分为真实URL和安全URL

@property
def DATABASE_URL(self) -> str:
    """实际连接用，含真实密码"""
    return (
        f"mysql+aiomysql://{self.MYSQL_USER}:{self.MYSQL_PASSWORD}@"
        f"{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
    )

@property
def DATABASE_URL_SYNC(self) -> str:
    """Alembic迁移用"""
    return (
        f"mysql+pymysql://{self.MYSQL_USER}:{self.MYSQL_PASSWORD}@"
        f"{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
    )

@property
def DATABASE_URL_SAFE(self) -> str:
    """日志打印用，密码遮盖"""
    return (
        f"mysql+aiomysql://{self.MYSQL_USER}:***@"
        f"{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
    )
```

**防密码泄露**: 搜索所有logger/print中使用DATABASE_URL的位置，改为DATABASE_URL_SAFE:
```bash
grep -rn "DATABASE_URL" app/ --include="*.py" | grep -v "SAFE\|SYNC\|def \|property"
```

---

### ⛔ C-06: chart_service.py db_url 用 *** 当密码 — 数据源连不上

**文件**: chart_service.py L80, L1165

```python
# L80
db_url = f"mysql+aiomysql://{db_model.username}:***@{db_model.host}:{db_model.port}/{db_model.database_name}"
# L95 — 用这个URL创建引擎
engine = create_async_engine(db_url, ...)

# L1165 同样
db_url = f"mysql+aiomysql://{db_model.username}:***@{db_model.host}:{db_model.port}/{db_model.database_name}"
```

**两个问题**:
1. 数据源引擎用 `***` 当密码 → 连不上有密码的外部数据库
2. 缓存key包含 `***` → 不同密码的同名数据源会命中错误缓存

**修复方案**:
```python
# L80 附近 — 用真实密码构建URL
decrypted_pwd = decrypt_password(db_model.password)
db_url = (
    f"mysql+aiomysql://{db_model.username}:{decrypted_pwd}@"
    f"{db_model.host}:{db_model.port}/{db_model.database_name}"
)

# L1165 同样修改
decrypted_pwd = decrypt_password(db_model.password)
db_url = (
    f"mysql+aiomysql://{db_model.username}:{decrypted_pwd}@"
    f"{db_model.host}:{db_model.port}/{db_model.database_name}"
)

# 如果需要日志打印，用不含密码的key
safe_url = f"mysql+aiomysql://{db_model.username}@{db_model.host}:{db_model.port}/{db_model.database_name}"
logger.debug(f"Connecting to data source: {safe_url}")
```

---

## 五、新增问题 (7个)

### ⛔ N-01 CRITICAL: sql_expressions.py build_filter_sql SQL注入

**文件**: sql_expressions.py L470-548

用户输入的 `val`、`range_start`、`range_end` 直接拼入SQL字符串，**零转义**:

```python
# L492 — 预设日期值未转义
clauses.append(f"{col} BETWEEN '{dates[0]}' AND '{dates[1]}'")

# L515 — 用户输入范围值未转义
clauses.append(f"{col} BETWEEN '{range_start}' AND '{range_end}'")

# L519-529 — 用户输入值未转义
clauses.append(f"{col} = '{val}'")
clauses.append(f"{col} != '{val}'")
clauses.append(f"{col} LIKE '%{val}%'")  # LIKE注入最危险

# L541 — IN列表未转义
items = ", ".join(f"'{v.strip()}'" for v in val.split(",") if v.strip())
```

**攻击示例**: 用户提交 filter value = `'; DROP TABLE users; --`
→ SQL变成: `WHERE col = ''; DROP TABLE users; --'`

这是**从engine.py拆出来时新引入的漏洞**，原有的 `_build_filter_sql` 在engine.py中也有同样问题（未修），现在又多了一份。

**修复方案**: 在sql_expressions.py中添加转义函数并应用

```python
# sql_expressions.py 顶部新增
def _escape_sql_string(value: str) -> str:
    """转义SQL字符串值中的特殊字符"""
    return (value or "").replace("\\", "\\\\").replace("'", "''")

# 然后所有 val/range_start/range_end/dates 替换:
# L492
- clauses.append(f"{col} BETWEEN '{dates[0]}' AND '{dates[1]}'")
+ clauses.append(f"{col} BETWEEN '{_escape_sql_string(str(dates[0]))}' AND '{_escape_sql_string(str(dates[1]))}'")

# L500
- clauses.append(f"{col} < '{dates[0]}'")
+ clauses.append(f"{col} < '{_escape_sql_string(str(dates[0]))}'")

# L508
- clauses.append(f"{col} > '{dates[1]}'")
+ clauses.append(f"{col} > '{_escape_sql_string(str(dates[1]))}'")

# L515
- clauses.append(f"{col} BETWEEN '{range_start}' AND '{range_end}'")
+ clauses.append(f"{col} BETWEEN '{_escape_sql_string(range_start)}' AND '{_escape_sql_string(range_end)}'")

# L519
- clauses.append(f"{col} = '{val}'")
+ clauses.append(f"{col} = '{_escape_sql_string(val)}'")

# L521
- clauses.append(f"{col} != '{val}'")
+ clauses.append(f"{col} != '{_escape_sql_string(val)}'")

# L523-529 同理，所有 '{val}' → '{_escape_sql_string(val)}'

# L531
- clauses.append(f"{col} LIKE '%{val}%'")
+ escaped = _escape_sql_string(val)
+ clauses.append(f"{col} LIKE '%{escaped}%'")
# 注意：LIKE的%和_也需要转义
+ escaped = _escape_sql_string(val).replace("%", "\\%").replace("_", "\\_")
+ clauses.append(f"{col} LIKE '%{escaped}%' ESCAPE '\\\\'")

# L533
- clauses.append(f"{col} LIKE '{val}%'")
+ escaped = _escape_sql_string(val).replace("%", "\\%").replace("_", "\\_")
+ clauses.append(f"{col} LIKE '{escaped}%' ESCAPE '\\\\'")

# L535
- clauses.append(f"{col} LIKE '%{val}'")
+ escaped = _escape_sql_string(val).replace("%", "\\%").replace("_", "\\_")
+ clauses.append(f"{col} LIKE '%{escaped}' ESCAPE '\\\\'")

# L541
- items = ", ".join(f"'{v.strip()}'" for v in val.split(",") if v.strip())
+ items = ", ".join(f"'{_escape_sql_string(v.strip())}'" for v in val.split(",") if v.strip())
```

---

### N-02 MEDIUM: sql_expressions.py _safe_identifier 只转义反引号

**文件**: sql_expressions.py L18-20

```python
def _safe_identifier(name: str) -> str:
    """安全地包裹表名/列名，避免 SQL 注入"""
    return f"`{name.replace('`', '``')}`"
```

只转义反引号不做白名单校验。而 temp_table_manager 和 watermark_manager 都已升级为白名单版。

**修复方案**: 统一使用白名单版本

```python
def _safe_identifier(name: str) -> str:
    """安全地包裹表名/列名，白名单校验"""
    raw = (name or "").strip()
    if not raw:
        raise ValueError("标识符不能为空")
    # 支持点号分隔 schema.table，以及反引号包裹的标识符
    clean = raw.replace("`", "").strip()
    if not re.match(r'^[a-zA-Z_\u4e00-\u9fff][\w\u4e00-\u9fff.]*$', clean):
        raise ValueError(f"非法标识符: {name}")
    parts = [p for p in clean.split(".") if p]
    escaped = [p.replace("`", "``") for p in parts]
    return ".".join(f"`{p}`" for p in escaped)
```

**注意**: 修改后需要测试所有调用 _safe_identifier 的地方，确认合法列名（含中文、数字）都能通过。同时需要在 sql_expressions.py 顶部确保 `import re`。

---

### N-03 MEDIUM: engine.py incremental_field 注入

**文件**: engine.py L503-504

```python
safe_col = f"`{incremental_field.replace('`', '')}`"
max_sql = f"SELECT MAX({safe_col}) FROM {tbl_name}"
```

`incremental_field` 来自 `node_config.get("incrementalField")` — 用户可控。只去反引号不校验。`tbl_name` 也未走 `_safe_table_name()`。

**修复方案**:
```python
# 先在engine.py中导入sql_expressions的_safe_identifier（修复后版本）
from app.services.pipeline.sql_expressions import _safe_identifier

# L503-504 替换为:
safe_col = _safe_identifier(incremental_field)
safe_tbl = self.temp_manager._safe_table_name(tbl_name)
max_sql = f"SELECT MAX({safe_col}) FROM {safe_tbl}"
```

---

### N-04 MEDIUM: chart_service.py DESCRIBE 注入

**文件**: chart_service.py L1044

```python
result = await conn2.execute(text(f"DESCRIBE `{table_name}`"))
```

`table_name` 来自正则解析用户SQL，未做白名单校验。

**修复方案**:
```python
# 使用已有的 _quote_mysql_identifier (L439已有白名单校验)
from app.services.chart_service import _quote_mysql_identifier  # 如果在同一文件直接用

safe_tbl = _quote_mysql_identifier(table_name)
result = await conn2.execute(text(f"DESCRIBE {safe_tbl}"))
```

或者更好的方式，加一个白名单校验:
```python
if not re.match(r'^[a-zA-Z_][a-zA-Z0-9_.]*$', table_name.replace('`', '')):
    raise ValueError(f"非法表名: {table_name}")
```

---

### N-05 MEDIUM: temp_table_manager.py L657 孤儿表清理注入

已在S-02部分修复中覆盖，同一处代码。

---

### N-06 LOW: canonical_pipeline_node_type 重复定义

已在S-09部分修复中覆盖。

---

### N-07 LOW: crypto.py 解密失败静默回退

已在S-04部分修复中覆盖。

---

## 六、原有计划中未涉及但仍需要的优化

### R-01: 宽泛 except Exception 162处

数量未变。建议按模块分批处理:
1. temp_table_manager.py (8处) → SQLAlchemyError + OSError
2. chart_service.py (6处) → SQLAlchemyError + ValueError + KeyError
3. engine.py (20+处) → 按每个except的实际可能异常类型替换
4. 其余文件逐步替换

### R-02: 类型提示覆盖率 43.6% (190/436)

比上次(42%)仅微升。需要系统化补全，特别是:
- engine.py: 约28个方法缺返回类型
- pipeline_service.py: 约15个方法
- permission_service.py: 约10个方法

### R-03: 测试覆盖

仍然是3个测试文件(test_crypto/test_exceptions/test_sql_injection)，核心业务零测试。建议第一批先给新增的修复加测试:
- test_config_security.py — SECRET_KEY空值拒绝启动
- test_safe_table_name.py — _safe_table_name白名单
- test_safe_identifier.py — _safe_identifier白名单
- test_build_filter_sql.py — build_filter_sql值转义

---

## 七、执行优先级 (按紧急程度排序)

### 🔴 P0 — 立即修复 (应用可能无法连接数据库)

| # | 任务 | 文件 | 预计耗时 |
|---|------|------|---------|
| 1 | DATABASE_URL改为真实密码 + 新增DATABASE_URL_SAFE | config.py | 15min |
| 2 | DATABASE_URL_SYNC同步修复 | config.py | 5min |
| 3 | chart_service.py db_url用真实密码 | chart_service.py L80, L1165 | 15min |
| 4 | 检查日志中是否泄露DATABASE_URL | 全局搜索 | 10min |

### 🔴 P0 — 立即修复 (可被攻击的SQL注入)

| # | 任务 | 文件 | 预计耗时 |
|---|------|------|---------|
| 5 | build_filter_sql 添加 _escape_sql_string + 所有值转义 | sql_expressions.py L470-548 | 30min |
| 6 | sql_expressions.py _safe_identifier 升级为白名单 | sql_expressions.py L18-20 | 15min |
| 7 | engine.py incremental_field 使用 _safe_identifier | engine.py L503 | 5min |
| 8 | chart_service.py DESCRIBE 加白名单校验 | chart_service.py L1044 | 5min |

### 🟡 P1 — 本周修复

| # | 任务 | 文件 | 预计耗时 |
|---|------|------|---------|
| 9 | temp_table_manager L657 孤儿表走 _safe_table_name | temp_table_manager.py | 5min |
| 10 | canonical_pipeline_node_type 去重，统一到validator | sql_expressions.py + engine.py | 20min |
| 11 | Database模型列宽255→512 | visualization.py | 5min |
| 12 | crypto.py 解密失败抛异常 | crypto.py L89-96 | 10min |
| 13 | datasources.py update接口密码加密 | datasources.py | 15min |

### 🟢 P2 — 后续迭代

| # | 任务 | 说明 | 预计耗时 |
|---|------|------|---------|
| 14 | engine.py继续拆分 | 拆preview.py + step_executor.py | 4h |
| 15 | except Exception分批替换 | 162处，先处理核心模块 | 3h |
| 16 | 类型提示补全 | 从43.6%提升到70%+ | 4h |
| 17 | 测试覆盖 | P0修复项对应的测试 | 3h |

---

## 八、预估分数提升

完成P0后: **59.5 → 70** (安全性从58→75，架构从60→68)
完成P1后: **70 → 75** (安全性→78，可维护性→62)
完成P2后: **75 → 78+**

---

## 九、修改注意事项

1. **P0项目1-4（密码URL）**: 修改后必须重启服务验证数据库连接正常
2. **P0项目5-8（SQL注入）**: 修改后运行 `pytest tests/test_sql_injection.py` 验证
3. **所有修改必须逐项提交**: 一项改完验证再改下一项，不要批量改
4. **P1项目11（列宽变更）**: 需要Alembic迁移脚本或手动 `ALTER TABLE databases MODIFY COLUMN password VARCHAR(512)`
5. **向后兼容**: 所有安全增强都是"加严不改松"，不会破坏合法场景的功能
