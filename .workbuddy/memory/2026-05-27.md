# 2026-05-27

## UI 调整：侧边栏"管道测试"位置移动
- 将 MainLayout.tsx 中主导航区的"管道测试"移除
- 在底部管理区域（sidebar-footer）"数据源管理"下方添加"管道测试"
- 效果："管道测试"从主菜单（报表/仪表盘/图表管理 下方）移至底部管理区（用户管理/数据源管理 下方）
- Build 通过 ✅

## UI 调整：侧边栏导航项字体统一
- MainLayout.css 中统一 `.nav-item`、`.nav-item-bottom`、`.nav-text` 的字体定义
- 统一为 `font-size: 14px; font-weight: 500; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;`
- Build 通过 ✅

## UI 调整：页面主标题字体统一
- 统一三个管理页面的标题样式：用户权限管理、数据源管理、数据管道测试
- 统一使用 `<Title level={3} style={{ margin: 0, fontSize: 20, fontWeight: 600 }}>`
- PipelineTestPage：从 Card title 改为独立 Title 组件，按钮移至标题右侧
- UserManagementPage：level 从 4 改为 3，补充 fontSize/fontWeight
- DataSourceManagementPage：level 从 2 改为 3，补充 fontSize/fontWeight
- Build 通过 ✅

## UI 调整：四个管理页面布局结构统一（方案A）
- PipelineTestPage：从 inline style 改为 `dashboard-page` + `page-header` + `header-content` + `dashboard-content` 结构，import DashboardPage.css
- DataSourceManagementPage：清理 header-content 冗余 inline style（已由 CSS 定义）
- DashboardPage：`<h1>` 改为 `Typography.Title level={3}`，添加 Typography import，清理 header-content 冗余 inline style
- ChartsManagementPage：`Title level={2}` 改为 `level={3}`，补充 fontSize/fontWeight
- 所有页面标题统一：`Title level={3}, fontSize: 20, fontWeight: 600`
- Build 通过 ✅
