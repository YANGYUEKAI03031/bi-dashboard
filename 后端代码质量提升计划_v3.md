# 后端代码质量提升计划 v3

> 第三轮审核 | 2026-05-25 | 评分: 61.5/100  
> 前两轮: ~44 → 53.5 → 59.5 → 61.5  
> 项目规模: 54文件 / 16,535行 / 437函数

---

## 评分总览

| 维度 | 得分 | 满分 | 扣分明细 |
|------|------|------|----------|
| 安全性 | 16 | 30 | S-01(-8) S-02(-3) S-03(-1) S-04(-0.5) S-05(-1.5) |
| 可维护性 | 14.5 | 25 | M-01(-3.5) M-02(-2.5) M-03(-2) M-04(-1.5) M-05(-1) |
| 架构 | 13.5 | 20 | A-01(-4) A-02(-1.5) A-03(-1) |
| 代码规范 | 12.5 | 15 | C-05(-1.5) C-06(-1) |
| 性能 | 5 | 10 | P-01(-3) P-02(-2) |

---

## P0 — 数据库密码 `***` 硬编码（-8分，运行时BUG）

**问题**: config.py、chart_service.py、datasources.py 中构建MySQL连接URL时，密码位置写成 `***` 字面量。解密密码已赋值给变量但未使用，`create_async_engine(db_url)` 实际连的是 `user:***@host`，运行时必然连接失败。

**影响范围**: 
- config.py L30/L38 → session.py、pipeline.py、trigger_scheduler.py 的内部DB连接
- chart_service.py L80/L96 → 外部数据源查询
- chart_service.py L1166 → 字段级联查询
- datasources.py L49 → 测试连接

### 修复1: config.py L30（内部数据库异步URL）

文件: `app/core/config.py`

顶部添加:
```python
from urllib.parse import quote_plus
```

```python
# 当前 (L29-31):
    def DATABASE_URL(self) -> str:
        return (
            f"mysql+aiomysql://{self.MYSQL_USER}:***@{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
        )

# 替换:
    def DATABASE_URL(self) -> str:
        return (
            f"mysql+aiomysql://{self.MYSQL_USER}:{quote_plus(self.MYSQL_PASSWORD)}@{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
        )
```

### 修复2: config.py L38（内部数据库同步URL）

```python
# 当前 (L37-39):
    def DATABASE_URL_SYNC(self) -> str:
        """同步数据库 URL，用于 Alembic 迁移"""
        return (
            f"mysql+pymysql://{self.MYSQL_USER}:***@{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
        )

# 替换:
    def DATABASE_URL_SYNC(self) -> str:
        """同步数据库 URL，用于 Alembic 迁移"""
        return (
            f"mysql+pymysql://{self.MYSQL_USER}:{quote_plus(self.MYSQL_PASSWORD)}@{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"
        )
```

### 修复3: chart_service.py L79-80（外部数据源引擎缓存）

文件: `app/services/chart_service.py`

顶部添加:
```python
from urllib.parse import quote_plus
```

```python
# 当前 (L78-80):
    # 解密密码（支持双轨：加密和明文）
    decrypted_password = decrypt_password(db_model.password)
    db_url = f"mysql+aiomysql://{db_model.username}:***@{db_model.host}:{db_model.port}/{db_model.database_name}"

# 替换:
    # 解密密码（支持双轨：加密和明文）
    decrypted_password = decrypt_password(db_model.password)
    db_url = f"mysql+aiomysql://{db_model.username}:{quote_plus(decrypted_password)}@{db_model.host}:{db_model.port}/{db_model.database_name}"
```

### 修复4: chart_service.py L1165-1166（字段级联查询）

```python
# 当前 (L1164-1166):
            # 解密密码（支持双轨：加密和明文）
            decrypted_password = decrypt_password(db_model.password)
            db_url = f"mysql+aiomysql://{db_model.username}:***@{db_model.host}:{db_model.port}/{db_model.database_name}"

# 替换:
            # 解密密码（支持双轨：加密和明文）
            decrypted_password = decrypt_password(db_model.password)
            db_url = f"mysql+aiomysql://{db_model.username}:{quote_plus(decrypted_password)}@{db_model.host}:{db_model.port}/{db_model.database_name}"
```

### 修复5: datasources.py L47-49（测试连接）

文件: `app/api/v1/datasources.py`

顶部添加:
```python
from urllib.parse import quote_plus
```

```python
# 当前 (L46-51):
    # 解密密码（支持双轨：加密和明文）
    password = decrypt_password(db_model.password)
    return (
        f"mysql+aiomysql://{db_model.username}:***@{db_model.host}:{db_model.port}/{db_model.database_name}"
    )

# 替换:
    # 解密密码（支持双轨：加密和明文）
    password = decrypt_password(db_model.password)
    return (
        f"mysql+aiomysql://{db_model.username}:{quote_plus(password)}@{db_model.host}:{db_model.port}/{db_model.database_name}"
    )
```

---

## P1 — build_category_expr SQL注入（-3分）

**问题**: sql_expressions.py L997-1004，`value`、`label`、`min_val`、`max_val` 直接拼入SQL，无任何转义。

文件: `app/services/pipeline/sql_expressions.py`

```python
# 当前 (L992-1004):
        if condition_type == "range":
            min_val = condition.get("min")
            max_val = condition.get("max")
            conditions = []
            if min_val is not None:
                conditions.append(f"{source_column} >= {min_val}")
            if max_val is not None:
                conditions.append(f"{source_column} <= {max_val}")
            if conditions:
                when_parts.append(f"WHEN {' AND '.join(conditions)} THEN '{label}'")
        elif condition_type == "value":
            value = condition.get("value", "")
            when_parts.append(f"WHEN {source_column} = '{value}' THEN '{label}'")

# 替换:
        if condition_type == "range":
            min_val = condition.get("min")
            max_val = condition.get("max")
            conditions = []
            safe_label = _escape_sql_string(label)
            if min_val is not None:
                safe_min = _escape_sql_string(str(min_val)) if isinstance(min_val, str) else str(min_val)
                conditions.append(f"{source_column} >= '{safe_min}'")
            if max_val is not None:
                safe_max = _escape_sql_string(str(max_val)) if isinstance(max_val, str) else str(max_val)
                conditions.append(f"{source_column} <= '{safe_max}'")
            if conditions:
                when_parts.append(f"WHEN {' AND '.join(conditions)} THEN '{safe_label}'")
        elif condition_type == "value":
            value = str(condition.get("value", ""))
            safe_value = _escape_sql_string(value)
            safe_label = _escape_sql_string(label)
            when_parts.append(f"WHEN {source_column} = '{safe_value}' THEN '{safe_label}'")
```

---

## P2 — engine.py L411 反引号替换不一致（-1分）

**问题**: L411 用 `replace('`', '')` 只删反引号不转义，与L334/L1119的 `replace('`', '``')` 不一致。

文件: `app/services/pipeline/engine.py`

```python
# 当前 (L411):
                                f"`{c.replace('`', '')}` = VALUES(`{c.replace('`', '')}`)"

# 替换:
                                f"`{c.replace('`', '``')}` = VALUES(`{c.replace('`', '``')}`)"
```

---

## P2 — chart_service.py engine.dispose() 静默吞异常（-1.5分中占0.5）

**问题**: L90 `except Exception: pass` 丢弃所有异常，连接泄漏风险。

文件: `app/services/chart_service.py`

```python
# 当前 (L88-91):
        try:
            await engine.dispose()
        except Exception:
            pass

# 替换:
        try:
            await engine.dispose()
        except Exception as e:
            logger.warning("释放旧引擎失败: %s", e)
```

---

## P3 — 连接池缓存key不唯一（-5分，与P0关联）

**问题**: chart_service 用 db_url 作为缓存key，当前 `***` 导致同用户名的不同数据源key碰撞。P0修复后密码加入URL可部分缓解，但更稳健的方案是用数据源ID作key。

文件: `app/services/chart_service.py` `_get_db_engine` 函数整体替换:

```python
# 当前函数 (L75-103):
async def _get_db_engine(db_model) -> Any:
    """获取或创建缓存的数据源引擎（复用连接池，避免频繁建连）"""
    global _data_source_engines
    # 解密密码（支持双轨：加密和明文）
    decrypted_password = decrypt_password(db_model.password)
    db_url = f"mysql+aiomysql://{db_model.username}:***@{db_model.host}:{db_model.port}/{db_model.database_name}"

    # 检查缓存是否存在且未过期
    if db_url in _data_source_engines:
        engine, created_at = _data_source_engines[db_url]
        if time.time() - created_at < _ENGINE_EXPIRE_SECONDS:
            return engine
        # 过期了，释放旧引擎
        try:
            await engine.dispose()
        except Exception:
            pass
        del _data_source_engines[db_url]

    # 创建新引擎（使用连接池）
    engine = create_async_engine(
        db_url,
        pool_size=5,
        max_overflow=10,
        pool_recycle=3600,
        pool_pre_ping=True,
    )
    _data_source_engines[db_url] = (engine, time.time())
    return engine

# 替换:
async def _get_db_engine(db_model) -> Any:
    """获取或创建缓存的数据源引擎（复用连接池，避免频繁建连）"""
    global _data_source_engines
    # 解密密码（支持双轨：加密和明文）
    decrypted_password = decrypt_password(db_model.password)
    db_url = f"mysql+aiomysql://{db_model.username}:{quote_plus(decrypted_password)}@{db_model.host}:{db_model.port}/{db_model.database_name}"
    cache_key = f"ds_{db_model.id}"  # 用数据源ID作缓存key，避免密码变更后key重叠

    # 检查缓存是否存在且未过期
    if cache_key in _data_source_engines:
        engine, created_at = _data_source_engines[cache_key]
        if time.time() - created_at < _ENGINE_EXPIRE_SECONDS:
            return engine
        # 过期了，释放旧引擎
        try:
            await engine.dispose()
        except Exception as e:
            logger.warning("释放旧引擎失败: %s", e)
        del _data_source_engines[cache_key]

    # 创建新引擎（使用连接池）
    engine = create_async_engine(
        db_url,
        pool_size=5,
        max_overflow=10,
        pool_recycle=3600,
        pool_pre_ping=True,
    )
    _data_source_engines[cache_key] = (engine, time.time())
    return engine
```

---

## 中长期优化项（不阻塞运行，但影响评分上限）

| 优先级 | 编号 | 问题 | 扣分 | 建议 |
|--------|------|------|------|------|
| P4 | M-01 | engine.py 3191行/54方法 God Class | -3.5 | 继续拆分：sql_builder.py（SQL拼接）、node_executor.py（节点执行）|
| P4 | M-02 | 类型标注覆盖率44% | -2.5 | 逐步补全返回值注解，CI中加mypy --strict |
| P4 | M-03 | 测试覆盖仅3文件668行 | -2 | 补充chart_service、engine核心路径测试 |
| P5 | A-02 | chart_service 1248行未拆分 | -1.5 | 拆为query_service、field_service、engine_cache |
| P5 | S-04 | datasources无更新接口 | -0.5 | 添加PUT /datasources/{id} 支持密码轮换 |
| P5 | S-05 | 162处broad except Exception | -1.5 | 分批替换为具体异常类型 |
| P5 | M-05 | canonical_pipeline_node_type委托冗余 | -1 | 直接import使用 |

---

## 修复后预期评分

| 维度 | 修复前 | P0+P1后 | 全部修完后 |
|------|--------|---------|-----------|
| 安全性 | 16/30 | 26/30 | 28/30 |
| 可维护性 | 14.5/25 | 16/25 | 21/25 |
| 架构 | 13.5/20 | 18/20 | 19/20 |
| 代码规范 | 12.5/15 | 14/15 | 15/15 |
| 性能 | 5/10 | 10/10 | 10/10 |
| **总计** | **61.5** | **~84** | **~93** |

---

## 历史记录

| 轮次 | 日期 | 评分 | 变化 | 主要修复项 |
|------|------|------|------|-----------|
| v1 | 2026-05-12 | 53.5 | +9.5 | 密码bcrypt、JWT校验、identifier白名单 |
| v2 | 2026-05-19 | 59.5 | +6 | 7项安全修复、engine.py拆分(3835→3196) |
| v3 | 2026-05-25 | 61.5 | +2 | sql_expressions转义、safe_table_name、test_sql_injection |
