# BI-Dashboard 优化实施计划（安全版）

> **版本**：v1.0 | **日期**：2026-05-24  
> **项目路径**：`E:\bi-dashboard`  
> **核心原则**：**优化可以分阶段，功能绝不能缺失或出错**

---

## 0. 总则

### 0.1 不可妥协的约束

| 约束 | 说明 |
|------|------|
| **零功能回归** | 每一阶段完成后，必须通过该阶段的回归清单，才能进入下一阶段 |
| **一次只改一类风险** | 同一 PR/提交内，不同时做「密码改造 + SQL 重构 + engine 拆分」 |
| **先兼容、后删除** | 所有破坏性变更必须经历「双轨期」，验证通过后再删旧逻辑 |
| **先备份、后迁移** | 任何数据库变更前必须备份 |
| **可回滚** | 每个阶段必须写清回滚步骤，且回滚后系统仍可运行 |

### 0.2 变更节奏（推荐）

```
准备基线 → P0 安全（分 3 个小批次）→ P1 基础工程 → P2 重构 → P3 可选增强
```

**预计总工期**：4～5 周（按单人兼职估算；若全职可压缩至 3 周）

### 0.3 分支策略

```bash
# 从当前主开发分支切出
git checkout -b optimize/safe-baseline        # 阶段 0
git checkout -b optimize/p0-security-batch1   # 阶段 1
git checkout -b optimize/p0-security-batch2
# ... 每阶段独立分支，合并前必须跑完回归清单
```

**合并条件**：阶段负责人签字（或自测勾选）+ 回归清单 100% 通过 + 无 P0/P1 级 bug。

---

## 阶段 0：建立基线（必做，0.5 天）

> **目的**：改之前知道「什么是正常的」，改之后能对比。

### 0.1 环境快照

- [ ] 记录当前 Git commit hash
- [ ] 备份 MySQL 全库：
  ```bash
  mysqldump -u root -p users > backup/bi_dashboard_YYYYMMDD.sql
  ```
- [ ] 复制 `backend/.env` 到本地安全位置（勿提交 Git）
- [ ] 确认后端 `python launch_server.py` 正常（:8000）
- [ ] 确认前端 `npm start` 正常（:3000）

### 0.2 功能基线清单（改前跑一遍，改后每阶段都要跑）

用 **`杨悦凯`** 登录（密码见本地约定，勿写入 Git），逐项操作并记录「预期结果」：

> 已验证：该账号 `user_id=1`，角色为 **admin**，可覆盖管理员全功能回归。

#### 认证
- [ ] 登录成功，跳转 `/dashboard`
- [ ] 错误密码提示「用户名或密码错误」
- [ ] `GET /api/v1/auth/me` 返回当前用户
- [ ] 修改密码后，新密码可登录、旧密码不可

#### 数据源
- [ ] 数据源列表加载
- [ ] 新建数据源 + 连接测试成功
- [ ] 查看表列表、列信息

#### 图表
- [ ] 可视化构建器：选表 → 配图表 → 保存
- [ ] 图表管理：列表、编辑、删除
- [ ] 图表查询：bar / line / pie / metric 至少各测 1 个
- [ ] 带筛选器的图表查询（date_range、select、multi_select）

#### 仪表盘
- [ ] 仪表盘列表
- [ ] 新建仪表盘：拖入图表卡片
- [ ] 编辑布局、Tab 切换
- [ ] 全局筛选器 + 卡片绑定筛选器
- [ ] 保存后刷新，布局与数据一致

#### 报表
- [ ] 报表页列表与浏览
- [ ] 报表页关联仪表盘展示正常

#### 权限
- [ ] 管理员：用户管理（列表、创建用户、改角色）
- [ ] 普通用户：仅能看被授权的报表页
- [ ] 非管理员无法访问 `/user-management`、`/pipeline-test`

#### Pipeline（管理员）
- [ ] 创建 Pipeline：source → aggregate → output（最小链路）
- [ ] 各节点预览有数据
- [ ] 全量执行成功
- [ ] chart 节点同步到 visualization_cards（如有使用）
- [ ] 触发器配置保存（不一定测自动触发，但保存不能报错）

### 0.3 建立最小冒烟脚本（可选但强烈建议）

在 `backend/test/smoke/` 新建手工可运行的检查（即使暂不接入 CI）：

```
test_health.py       → GET /health 200
test_login.py        → POST /auth/login 200 + token
test_charts_list.py  → GET /visualization/charts 200（带 token）
```

> 当前项目几乎无自动化测试，**阶段 0 的基线清单就是安全网**。每阶段合并前必须完整跑一遍。

### 0.4 阶段 0 完成标准

- [ ] 数据库备份文件存在且可恢复（至少在测试库验证一次 `mysql < backup.sql`）
- [ ] 基线清单全部勾选通过
- [ ] 已知问题列表记录（改优化时不误当回归）

---

## 阶段 1：P0 安全加固（必做，第 1 周）

> **原则**：分 3 个小批次提交，每批次独立回归。  
> **禁止**：在 P0 期间改动 chart 查询逻辑结构（SQL 参数化单独一批）。

---

### 批次 1.1：密钥与日志清理（风险：低 | 耗时：0.5 天）

#### 改动范围

| 文件 | 操作 |
|------|------|
| `.gitignore` | 新增 `.env`、`venv/`、`__pycache__/`、`*.pyc` |
| `backend/.env.example` | 新建，全部用占位符 |
| Git | `git rm --cached backend/.env`（保留本地文件） |
| `backend/app/core/config.py` | 删除硬编码 `MYSQL_PASSWORD="603031"` 等默认值，改为从 `.env` 读取；本地 `.env` 填真实值 |
| `backend/app/api/v1/auth.py` | **删除** `logger.info` 中打印 `password` 的行 |
| `frontend/bi-dashboard/package.json` | 移除未使用的 `mysql2` |
| `frontend/.../authService.ts`、`AuthContext.tsx` | 删除或 `#if DEV` 包裹 `console.log` |

#### 实施步骤

1. 创建 `.env.example`（不含真实密钥）
2. 确保本地 `backend/.env` 有完整配置，服务能启动
3. 更新 `.gitignore` 并 `git rm --cached backend/.env`
4. 清理日志与 mysql2
5. 重启前后端，跑 **认证 + 登录** 基线项

#### 验证

- [ ] `git status` 不再追踪 `backend/.env`
- [ ] 后端启动无配置缺失报错
- [ ] 登录/登出正常
- [ ] 浏览器控制台无 token/密码输出
- [ ] `npm run build` 成功

#### 回滚

```bash
git revert <commit>
# 若 .env 被误删，从本地备份恢复
```

---

### 批次 1.2：密码 bcrypt 迁移（风险：中 | 耗时：1 天）

> **关键：双轨验证**——迁移后同时支持 `password_hash` 与旧 `password`，确认全员可用后再删明文。

#### 改动范围

| 文件 | 操作 |
|------|------|
| `backend/migrations/add_password_hash.sql` | 执行（`USE` 改为实际库名 `users`） |
| `backend/app/models/user.py` | 新增 `password_hash = Column(String(255), nullable=True)` |
| `backend/migrate_password_hash.py` | 运行，填充 hash |
| `backend/app/core/security.py` | 新增辅助函数 `check_password(plain, user)` |
| `backend/app/api/v1/auth.py` | 登录改用 `check_password` |
| `backend/app/api/v1/auth.py` | `change_password` 写入 `password_hash`，**暂不删** `password` |
| `backend/app/services/permission_service.py` | `create_user` 写 `password_hash = get_password_hash(password)` |

#### `check_password` 双轨逻辑（过渡期必用）

```python
def check_password(plain: str, user: User) -> bool:
    if user.password_hash:
        return verify_password(plain, user.password_hash)
    # 过渡：旧用户尚未迁移时回退明文
    return user.password is not None and user.password == plain
```

#### 实施步骤

1. **备份数据库**
2. 执行 SQL 添加 `password_hash` 列
3. 更新 User 模型
4. 运行 `python migrate_password_hash.py`
5. 改 auth / permission_service / change_password
6. 测试：admin 登录、新建用户登录、改密后登录
7. **观察 3～7 天**（或内部全量测一轮）后，再执行批次 1.2b

#### 验证

- [ ] 已有用户（admin）登录成功
- [ ] 新建用户登录成功
- [ ] 修改密码：新密码 OK，旧密码失败
- [ ] DB 中 `password_hash` 非空且以 `$2b$` 开头
- [ ] 禁用账户仍返回 403

#### 批次 1.2b：删除明文字段（稳定后单独提交）

| 操作 | 说明 |
|------|------|
| 删除 `User.password` 列 | `ALTER TABLE useraccount DROP COLUMN password` |
| 删除 `check_password` 明文回退 | 仅保留 `verify_password` |
| 删除 `migrate_password_hash.py` 中的明文依赖注释 |

**前置条件**：确认所有用户 `password_hash` 均已填充。

#### 回滚

```sql
-- 代码回滚到批次 1.2 之前
-- 数据库保留 password 列即可继续明文登录
git revert <commit>
```

---

### 批次 1.3：SQL 参数化（风险：高 | 耗时：2～3 天）

> **这是 P0 中风险最高的一项。必须单独分支、单独回归，且不改 engine.py。**

#### 改动范围

| 文件 | 操作 |
|------|------|
| `backend/app/services/chart_service.py` | 筛选器 SQL 值改参数绑定；列名白名单 |
| `backend/app/services/pipeline/temp_table_manager.py` | 标识符白名单；INSERT 值参数化 |
| `backend/app/services/pipeline/trigger_scheduler.py` | 字段白名单 + 值参数化 |

#### 实施策略

**第一步：抽工具函数（不改行为）**

新建 `backend/app/core/sql_utils.py`：

```python
import re
IDENTIFIER_RE = re.compile(r'^[a-zA-Z_][a-zA-Z0-9_]*$')

def validate_identifier(name: str) -> str:
    if not name or not IDENTIFIER_RE.match(name):
        raise ValueError(f"Invalid SQL identifier: {name!r}")
    return name

def quote_identifier(name: str) -> str:
    return f"`{validate_identifier(name)}`"
```

**第二步：改 `_metric_filter_sql_clauses`**

- 返回值从 `List[str]` 改为 `Tuple[List[str], Dict[str, Any]]`
- 所有 **值** 用 `:param_name` 占位
- 所有 **字段名** 走 `quote_identifier`

**第三步：改 `execute_chart_query` 合并 params**

- 收集 filter params + date params + metric params
- 一次 `execute(text(sql), all_params)`

**第四步：Pipeline 侧**

- 表名/列名：`validate_identifier`
- JSON 插入：优先 `json.dumps` + 单参数绑定

#### 分步验证（每步都要测）

| 步骤 | 必测场景 |
|------|----------|
| 工具函数 | 非法列名抛错 |
| metric 筛选 | 等于、大于、包含、为空 |
| dashboard 筛选 | date_range、date_relative、select、multi_select |
| 图表类型 | bar、line、pie、metric、stacked_bar |
| Pipeline | source 预览、aggregate、join、filter、全量执行 |

#### 验证

- [ ] 基线清单「图表 + 仪表盘 + Pipeline」全部通过
- [ ] 故意传入 `'; DROP TABLE--` 类筛选值，应被参数化（表仍在）
- [ ] 日志无 SQL 语法错误

#### 回滚

```bash
git revert <batch-1.3-commit>
# 不涉及 DB 结构，回滚代码即可
```

---

## 阶段 2：P1 基础工程（必做，第 2 周）

> **原则**：提升可维护性，不改变对外 API 行为（除非明确标注）。

---

### 2.1 数据源密码加密（风险：中 | 耗时：1 天）

#### 改动范围

| 文件 | 操作 |
|------|------|
| `backend/app/core/crypto.py` | Fernet 加解密 |
| `backend/app/models/visualization.py` | `Database.password` 存密文 |
| `backend/app/api/v1/datasources.py` | 创建/更新加密；返回脱敏 |
| `backend/migrations/encrypt_datasource_passwords.py` | 一次性迁移脚本 |

#### 双轨策略

```python
def get_datasource_password(db_row) -> str:
  try:
    return decrypt(db_row.password)
  except Exception:
    return db_row.password  # 迁移前的明文
```

#### 验证

- [ ] 已有数据源连接测试仍成功
- [ ] 新建数据源连接测试成功
- [ ] 图表查询仍正常（依赖数据源连接）
- [ ] API 响应不含明文密码
- [ ] DB 中 password 字段非可读明文

#### 回滚

- 代码 revert
- 若有迁移脚本备份，可还原明文（保留迁移前 DB 备份）

---

### 2.2 userID 自增（风险：低 | 耗时：0.5 天）

#### 改动范围

| 文件 | 操作 |
|------|------|
| `backend/app/models/user.py` | `autoincrement=True` |
| `backend/app/services/permission_service.py` | 删除 `MAX(userID)+1` |
| SQL 迁移 | `ALTER TABLE useraccount MODIFY userID INT AUTO_INCREMENT` |

#### 验证

- [ ] 连续创建 3 个用户，ID 不重复
- [ ] 已有用户 ID 不变
- [ ] 外键（图表 created_by 等）不受影响

#### 回滚

```sql
-- 一般不需要回滚；若必须，恢复手动 ID 逻辑 + 去掉 AUTO_INCREMENT
```

---

### 2.3 自定义异常 + 全局处理器（风险：低 | 耗时：1 天）

#### 改动范围

| 文件 | 操作 |
|------|------|
| `backend/app/exceptions.py` | 新建异常体系 |
| `backend/app/main_optimized.py` | 注册 `@app.exception_handler` |
| `backend/app/services/permission_service.py` | 优先改（用户可见错误多） |
| 其他 services | 渐进替换，**保留 HTTPException 兼容** |

#### 兼容性要求

- 现有前端依赖 `detail` 字符串的，新格式需同时兼容：
  ```json
  {"detail": "用户名已存在"}
  // 或
  {"detail": {"code": "USER_EXISTS", "message": "用户名已存在"}}
  ```
- **第一阶段**：仅对 NEW 异常用结构化格式；旧 `HTTPException` 不动

#### 验证

- [ ] 故意触发 404、403、400，前端 message 仍正常展示
- [ ] 500 错误不泄露 SQL/堆栈给前端（DEBUG=false 时）

---

### 2.4 引入 Alembic（风险：中 | 耗时：1 天）

#### 改动范围

```bash
cd backend
alembic init alembic
# 配置 env.py 导入 Base + 所有 models
# alembic.ini sqlalchemy.url 从 settings 读取
```

#### 关键：已有库的 baseline 策略

```bash
# 1. autogenerate 生成 baseline revision
alembic revision --autogenerate -m "baseline"

# 2. 检查生成的 migration，删掉多余 DROP/CREATE

# 3. 标记当前 DB 已是 head（不要对生产库执行 upgrade 建表）
alembic stamp head
```

#### 验证

- [ ] `alembic current` 显示 head
- [ ] 新建一个空列 migration（如测试用），`upgrade` + `downgrade` 可逆
- [ ] 现有功能基线清单通过

#### 回滚

- 删除 `alembic/` 目录和 `alembic.ini`（不影响已有数据）

---

### 2.5 配置外部化 + 小修复（风险：低 | 耗时：0.5 天）

| 文件 | 操作 |
|------|------|
| `backend/app/core/config.py` | 新增 `CHART_QUERY_LIMIT`、`PIPELINE_BATCH_SIZE`、`CACHE_TTL`、`TEMP_TABLE_RETENTION_MINUTES` |
| `chart_service.py` / `engine.py` | 替换硬编码 magic number |
| `main_optimized.py` | `health` 用 `text("SELECT 1")`；timestamp 用 `datetime.now(timezone.utc)` |
| `config.py` | 修复 Redis URL 无密码格式 `redis://host:port/db` |

#### 验证

- [ ] `/health` 返回 database connected
- [ ] 图表查询结果行数与改前一致
- [ ] Pipeline 执行结果与改前一致

---

### 2.6 清理死代码（风险：低 | 耗时：0.5 天）

| 操作 | 条件 |
|------|------|
| 删除 `backend/main.py` | 确认 `launch_server.py` 仅引用 `main_optimized` |
| 删除空文件 / 过时迁移脚本 | Alembic 稳定运行后 |
| 补全 `models/__init__.py` | 便于 Alembic autogenerate |

**不做**：计划中的 `endpoints/user.py`（项目中不存在；用户 API 已在 `permissions.py`）

#### 验证

- [ ] 后端启动路径不变
- [ ] 基线清单通过

---

## 阶段 3：P2 核心重构（必做，第 3～4 周）

> **原则**：engine.py 拆分是「一次只抽一个模块」；每抽完一个模块，完整跑 Pipeline 回归。

---

### 3.1 engine.py 拆分（风险：高 | 耗时：5～7 天）

#### 目标结构

```
backend/app/services/pipeline/
├── engine.py           # 仅编排 run()，目标 < 800 行
├── validator.py        # 拓扑排序、节点校验、SQL 校验
├── type_inferrer.py    # 列类型推断
├── sql_builder.py      # SQL 构建
└── step_executor.py    # 执行、批处理、临时表写入
```

#### 拆分顺序（严格按序）

| 步骤 | 抽出模块 | 风险 | 验证重点 |
|------|----------|------|----------|
| 3.1a | `validator.py` | 低 | 非法 DAG 仍报错；合法 DAG 排序不变 |
| 3.1b | `type_inferrer.py` | 低 | 各节点 preview schema 不变 |
| 3.1c | `sql_builder.py` | 中 | 各节点生成 SQL 与改前一致（可 snapshot 对比） |
| 3.1d | `step_executor.py` | 高 | 全量执行、增量、水位线 |

#### 安全做法

1. **复制而非重写**：先把函数原样移到新文件，`engine.py` import 调用
2. **不改函数签名**（第一阶段）
3. 每步提交独立，message 如 `refactor(pipeline): extract validator from engine`
4. 对比测试：同一 Pipeline JSON，改前改后 `execution.result` 一致

#### Pipeline 回归清单（每步必跑）

- [ ] source → output 最小链路
- [ ] source → aggregate → output
- [ ] source → join → output
- [ ] source → filter → aggregate → output
- [ ] transpose / deduplicate / column_select 各 1 个
- [ ] chart 节点 + sync 到 visualization_cards
- [ ] 节点 preview 与 execute 数据一致

#### 回滚

- 每步一个 commit，回滚到上一步即可

---

### 3.2 临时表自动清理（风险：低 | 耗时：0.5 天）

| 文件 | 操作 |
|------|------|
| `temp_table_manager.py` | `cleanup_all_expired()`、`cleanup_orphaned_on_startup()` |
| `main_optimized.py` | lifespan 启动时调用 |
| `core/scheduler.py` | 每小时定时清理 |

#### 验证

- [ ] 重启服务后，超过 retention 的 `tmp_pipeline_*` 被清理
- [ ] 正在执行的 Pipeline 临时表不被误删（命名含 exec_id，清理需判断时间）

---

### 3.3 datetime.utcnow 统一替换（风险：低 | 耗时：0.5 天）

| 操作 | 说明 |
|------|------|
| 新建 `app/core/time_utils.py` | `utc_now()` |
| 全局替换 | models defaults + services |

#### 注意

- SQLAlchemy `onupdate=datetime.utcnow` 改为 `utc_now` 或 `server_default=func.now()`
- **不改数据库已有数据的时区含义**，仅消除 DeprecationWarning

#### 验证

- [ ] 创建/更新资源，`created_at`/`updated_at` 仍合理
- [ ] Python 3.12 启动无 utcnow 警告

---

## 阶段 4：P3 可选增强（按需，第 5 周及以后）

> **默认不做**，除非 P0～P2 全部完成且业务明确需要。

### 4.1 统一分页格式（可选）

- **风险**：破坏现有前端解析
- **做法**：仅新 API 使用 `PageResponse`；旧 API 保持原样
- **前置**：前端封装统一 `parseListResponse()`

### 4.2 事件总线解耦（暂缓）

- 当前 inline import 规模可控
- 替代方案：提取 `pipeline_chart_sync_service` 为显式编排，由 API 层调用

### 4.3 RealTimeSync 完整实现（暂缓）

- 与 TriggerScheduler、Redis 缓存重叠
- 若要做，优先做「Pipeline 执行进度 SSE」，而非 binlog 全表监听

### 4.4 前端 axios 统一客户端（建议但非阻塞）

- 统一 token、错误处理
- 与 P0/P1 独立，可在 P2 期间并行

---

## 5. 每阶段合并前总检清单

```
[ ] 本阶段所有子任务验证项已通过
[ ] 阶段 0 功能基线清单完整重跑通过
[ ] 无新增 console 敏感日志
[ ] 后端 DEBUG=false 下启动正常
[ ] npm run build 成功
[ ] 数据库 migration 可回滚或有备份
[ ] 已更新本文档对应章节的完成日期
```

---

## 6. 风险矩阵与决策记录

| 任务 | 功能影响风险 | 建议 |
|------|-------------|------|
| 密码双轨 | 低（设计得当） | 必做 |
| SQL 参数化 | **高** | 单独分支，充分回归 |
| engine 拆分 | **高** | 每次只抽 1 模块 |
| 数据源加密 | 中 | 双轨 decrypt |
| userID 自增 | 低 | 必做 |
| Alembic stamp | 中 | 勿对已有库 blind upgrade |
| 删 password 列 | 中 | 稳定 1 周后再做 |
| RealTimeSync | 高（新 bug 面） | 暂缓 |

---

## 7. 执行日历（建议）

| 周 | 批次 | 内容 | 合并门槛 |
|----|------|------|----------|
| W0 | 阶段 0 | 基线 + 备份 | 基线清单全绿 |
| W1 前 2 天 | 1.1 + 1.2 | 密钥/日志 + 密码 bcrypt | 认证回归 |
| W1 后 3 天 | 1.3 | SQL 参数化 | 图表+Pipeline 回归 |
| W2 | 2.1～2.6 | 数据源加密、userID、异常、Alembic、配置 | 全基线 |
| W3 | 3.1a～b | engine validator + type_inferrer | Pipeline 回归 |
| W4 | 3.1c～d + 3.2～3.3 | sql_builder + step_executor + 清理 | 全基线 |
| W5+ | P3 | 按需 | 单独立项 |

---

## 8. 暂缓 / 剔除项（避免无效工作）

| 原优化计划项 | 处理 |
|-------------|------|
| 4.2 完善 `endpoints/user.py` | **剔除**——已在 `permissions.py` 实现 |
| 4.1 RealTimeSync binlog | **暂缓**——投入大、价值不明确 |
| 3.1 事件总线 | **暂缓**——当前规模不需要 |
| 3.2 统一分页 | **可选**——需前端协同，非阻塞 |

---

## 9. 协作约定（后续按此 plan 改代码时）

1. **每次开始任务**：先读本文档对应章节 → 创建分支 → 备份 DB（若涉及）
2. **每次提交**：单一职责，message 注明阶段编号（如 `P0-1.2: bcrypt login`）
3. **每次合并**：勾选第 5 节总检清单
4. **遇到基线失败**：停止后续阶段，先修复或回滚，不「带病前进」
5. **功能与优化冲突时**：保功能，优化让步或缩小 scope

---

## 附录 A：关键文件索引

| 模块 | 路径 |
|------|------|
| 入口 | `backend/launch_server.py` → `app/main_optimized.py` |
| 认证 | `backend/app/api/v1/auth.py` |
| 图表查询 | `backend/app/services/chart_service.py` |
| Pipeline 引擎 | `backend/app/services/pipeline/engine.py` |
| 权限 | `backend/app/services/permission_service.py` |
| 前端路由 | `frontend/bi-dashboard/src/App.tsx` |
| 配置 | `backend/app/core/config.py` |

## 附录 B：数据库备份与恢复

```bash
# 备份
mysqldump -u root -p users > backup/users_YYYYMMDD_HHMM.sql

# 恢复到测试库（勿直接覆盖生产）
mysql -u root -p users_test < backup/users_YYYYMMDD_HHMM.sql
```

---

**下一步行动**：从 **阶段 0（基线 + 备份）** 开始。阶段 0 完成后，按 **批次 1.1** 执行第一项代码改动。
